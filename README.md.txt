# مدیریت هوشمند ذوب

یک اپلیکیشن تحت وب ساده برای ریاست ذوب.  
ساخته شده با FastAPI + SQLite + Jinja2.

## نصب و اجرا
1. Python 3.11+ نصب کنید.
2. پیش‌نیازها:
   ```bash
   pip install -r requirements.txt
